
Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	Отказ = Истина;
	Сообщить("Установщик не поддерживает обычное приложение. Используйте управляемое приложение.");
	//! ЭтотОбъект = 0; // ВнешняяОбработкаОбъект
	Попытка
		ИспользуемоеИмяФайла = ЭтотОбъект.ИспользуемоеИмяФайла;
	Исключение
		// Не портативный режим
		Возврат;
	КонецПопытки;
	ЗапуститьСистему("/RunModeManagedApplication /Execute""" + ИспользуемоеИмяФайла + """");
КонецПроцедуры

