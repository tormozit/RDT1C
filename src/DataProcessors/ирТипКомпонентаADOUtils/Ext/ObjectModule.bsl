#Если Сервер И Не Сервер Тогда
Перем Перечисления Экспорт; // - только чтение. Объект, одноименными свойствами которого, являются объекты перечислений из библиотеки типов ADO. Объекты перечислений имеют одноименные свойства значений перечислений из библиотеки типов ADO.
// Предназначено для получения различных констант ADO, заданных перечислениями в библиотеки типов. Версия библиотеки типов должна быть 2.x.
#КонецЕсли

Перем ДвоичныеДанныеВВидеСтрок Экспорт; // Булево - чтение и запись. Если Истина, то в таблице значений (результате запроса) данные типов adBinary, adVarBinary, adLongVarBinary будут приниматься в виде строк с 
// шестнадцатиричной записью массива байтов с префиксом "0x", соответствующим колонкам ТЗ будет назначен тип Строка(0). Максимальная длина отображаемых данных для типа adLongVarBinary 8 килобайт. 
// Если Ложь, то никакого преобразования не будет. (В этом случае ADO возвращает SafeArray VT_UI1.) После создания объекта значение свойства - Ложь.

// См. Обработка.ирПлатформа.Макет.GameWithFire_Описание
// Метод создает новую таблицу значений с колонками соответствующими колонкам переданного набора записей. 
// И переносит содержимое набора записей в таблицу значений. При создании колонок в таблице значений, им назначается тип соответствующий типу поля ADORecordset.
// Соответствие типов:
//	 adChar, adVarChar, adWChar, adVarWChar, adBSTR: Строка(n); где n - DefinedSize, переменная длина;
//	 adDecimal, adNumeric: Число(p, s), где p - Precision; s - Scale; любой знак;
//	 adUnsignedTinyInt: Число(3, 0), любой знак;
//	 adSmallInt, adUnsignedSmallInt: Число(5, 0), любой знак;
//	 adInteger, adUnsignedInt: Число(10, 0), любой знак;
//	 adBigInt, adUnsignedBigInt: Число(20, 0), любой знак;
//	 adDate, adDBDate, adDBTime, adDBTimeStamp: Дата, дата и время;
//	 adBoolean: Булево.
// А также колонка может содержать тип Null.
// Для остальных типов ADO тип колонки таблицы значений не назначается.
// Правила назначения имени колонки: если имя в результате запроса пустое, назначается имя '_', если в имени есть символы не буквы и не цифры они заменяются на символ '_', если первый символ имени цифра, он заменяется на символ '_'; затем, если получилось такое имя, которое уже присутствует в коллекции колонок, то имя новой колонки будет образовано путем добавления суффикса с индексом начиная от 1.// Сравнить строки
// Параметры:
//   ADORecordset - _Recordset {ADODB.Connection}
// Возвращаемое значение:
//   ТаблицаЗначений 
Функция ADORecordsetToValueTable(Знач ADORecordset) Экспорт
КонецФункции   
